{
  "id": "tool.feedback-log",
  "name": "Feedback Logging",
  "version": "0.1.0",
  "description": "Lets TPM capture quick user feedback / ideas to a persistent JSON log the user later triages in the Settings panel. Opt-in. The `feedbackFilePath` parameter exposed to the agent is injected at runtime by the extension from `context.globalStorageUri` — it is not a user-editable setting and does not appear in the Modules tab.",
  "proactive": false,
  "contributes": {
    "promptFragments": [
      { "target": "tpm", "contentPath": "feedback.md" }
    ]
  }
}
